# 任意：GitHub Pages で配信したい場合だけ有効にする。
# raw.githubusercontent.com から取る運用なら、このワークフローは不要（削除してよい）。
# Pages を使う利点は独自ドメイン・キャッシュ制御。欠点は設定が1つ増えること。
name: publish-pages

on:
  push:
    branches: [main]
    paths:
      - "public/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: public
      - id: deployment
        uses: actions/deploy-pages@v4
